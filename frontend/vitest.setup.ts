import '@testing-library/jest-dom/vitest'

// Publication-unit tests exercise exact approved fixtures. The checked-in
// production decision stays closed until qualified review is complete.
process.env.UK_PROFESSIONAL_OPPORTUNITIES_PUBLIC_DATA_ENABLED = 'true'
